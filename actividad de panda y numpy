import numpy as np
import pandas as pd

random_numbers = np.random.randint(1, 101, 10)

print("Lista aleatoria de 10 números:")
print(random_numbers)



if 'random_numbers' not in locals():
    print("Error: La lista 'random_numbers' no ha sido generada. Por favor, ejecuta la celda anterior primero.")
else:

    series_from_list = pd.Series(random_numbers, index=range(1, 11))

    print("Serie de pandas creada:")
    display(series_from_list)

if 'series_from_list' not in locals():
    print("Error: La serie 'series_from_list' no ha sido generada. Por favor, ejecuta las celdas anteriores primero.")
else:
   
    squares_of_series = series_from_list ** 2

    print("Cuadrados de los valores de la serie:")
    display(squares_of_series)

if 'squares_of_series' not in locals():
    print("Error: La serie 'squares_of_series' no ha sido generada. Por favor, ejecuta las celdas anteriores primero.")
else:
    
    print("Últimos cuatro elementos de la serie de cuadrados:")
    display(squares_of_series.tail(4))
